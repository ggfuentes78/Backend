html 
    head 
        title Productos
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous")
    body(class="bg-dark text-white")
        h1(class="ms-2 mt-5") Productos
        if productos.length==0 
            H3(class="ms-4 mt-5") No hay productos
        else
            table(class="table table-dark table-striped ms-5 px-2 mt-5 align-middle" style="width:40%;")
                tbody 
                    tr
                        th(id="id" class="fs-4") ID 
                        th(id="title" class="fs-4") TITULO
                        th(id="price" class="fs-4") PRECIO 
                        th(id="thumbnail" class="fs-4") TAPA
                    each prod in productos
                        tr
                            td(class="fs-5")=prod.id 
                            td(class="fs-5")=prod.nombre 
                            td(class="fs-5")='$'+prod.price 
                            td
                                img(src=prod.thumbnail class="img-thumbnail" alt=prod.title)
        form(class="ms-4 mt-5" method="link" action="../" style="width:50%;")
            div
                button(type="submit" class="btn btn-primary") Cargar Producto